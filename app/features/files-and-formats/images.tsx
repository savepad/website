import React from 'react';
import Image from 'next/image';

const ImagesFeature = () => (
  <section id="images" className="mb-12 max-w-full">
    <h3>Images</h3><br></br>

    <p>Upload image files like photos, diagrams, and screenshots without hassles to Savepad to search for later. Savepad recognizes common image formats like .jpeg, .png, and .gif. Depending on the format, the content, and the file, Savepad may auto-enrich the file to transcribe the content, making text in images searchable vis Savepad&apos;s full-text search.</p><br></br>

  </section>
);

export default ImagesFeature;
