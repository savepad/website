import React from 'react';
import Image from 'next/image';

const EmailsFeature = () => (
  <section id="emails" className="mb-12 max-w-full">
    <h3>Emails</h3><br></br>

    <p>Each Savepad user gets a unique email address — you can find this under user settings — to save emails that caught your attention. Rather than screenshotting emails, simply forward emails to that address, and Savepad will save the content, metadata, and context, making it easier to find when you search for it.</p><br></br>

    <p><i>Currently under development</i></p><br></br>
  </section>
);

export default EmailsFeature;
