import React from 'react';
import Image from 'next/image';

const BacklogFeature = () => (
  <section id="backlog" className="mb-12 max-w-full">
    <h3>Backlog</h3><br></br>

    
    <p>By default, if you add anything to your Savepad account without any context - titles, descriptions, notes, tags, etc. - they&apos;re added to your backlog. This works for anything added from the web app, browser extensions, apps, or integrations. Every once in a while we&apos;ll nudge you to make sure your backlog is cleaned up.</p><br></br>
  </section>
);

export default BacklogFeature;
